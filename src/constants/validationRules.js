export const validataionRules = {
    RUSSIA_WORDS: /[А-ЯЁа-яё]+$/,
    ENGLISH_WORDS:/^[A-Za-z]+$/,
    EMAIL: /[A-Za-z0-9]+@[A-Za-z0-9]+\.[A-Za-z]+/,
    ONLY_6_NUMB: /^\d{6}$/,
    ONLY_4_NUMB: /^\d{4}$/,
    ONLY_NUMB: /^\d$/ 
}